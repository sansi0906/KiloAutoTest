# 津筑网 App 自动化测试系统依赖包

# 核心依赖
# 无需额外依赖，使用Python标准库

# 可选依赖（用于扩展功能）
# Pillow>=9.0.0  # 图片处理（如需要图片压缩或格式转换）
# beautifulsoup4>=4.10.0  # HTML解析（如需要更复杂的报告生成）
# jinja2>=3.0.0  # 模板引擎（如需要更灵活的报告模板）
# pytest>=7.0.0  # 测试框架（如需要使用pytest）
# allure-pytest>=2.9.0  # Allure测试报告（如需要生成Allure报告）

# 开发依赖
# black>=22.0.0  # 代码格式化
# flake8>=4.0.0  # 代码检查
# mypy>=0.910  # 类型检查